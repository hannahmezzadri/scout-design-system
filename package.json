{
  "name": "scout",
  "version": "0.0.0",
  "private": true,
  "description": "Scout — enterprise design system (root)",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "build:tokens": "pnpm --filter @scout/tokens build",
    "clean": "turbo run clean",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.6.3"
  }
}
