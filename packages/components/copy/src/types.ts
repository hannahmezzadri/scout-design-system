/** Copy control size — mirrors scout-control. */
export type CopySize = 'default' | 'condensed';

/** Visual layout — icon-only or icon + label. */
export type CopyLayout = 'icon-only' | 'icon-label';
