/**
 * Text-field variants. The variant attr drives input type, formatting, and
 * affordances (icons, masking, popover triggers).
 */
export type TextFieldVariant =
  | 'text'
  | 'number'
  | 'confirmation'
  | 'currency'
  | 'password'
  | 'phone'
  | 'search'
  | 'sensitive-data'
  | 'date-picker'
  | 'month-picker'
  | 'time-picker';

export type TextInputSize = 'default' | 'condensed';
