/** Layout density. */
export type AddressSize = 'full' | 'condensed' | 'single-line';

/** Optional selector rendered to the left of the address. */
export type AddressSelectTool = 'none' | 'checkbox' | 'radio';

/** Layout direction of the label and body. */
export type AddressOrientation = 'stacked' | 'inline';
