/**
 * Convenience presets for common skeleton shapes. Width / height / radius
 * can also be set explicitly via attributes for any custom shape.
 */
export type SkeletonShape = 'line' | 'block' | 'circle';
