/**
 * Notification badge sizes:
 * - `xx-small` (6px) — barebones dot, no number
 * - `x-small`  (8px) — slightly larger dot, no number
 * - `small`    (16px) — small badge with 1–2 digit number
 * - `medium`   (20px) — medium badge with 1–2 digit number
 */
export type NotificationBadgeSize = 'xx-small' | 'x-small' | 'small' | 'medium';
