{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "@scout/docs (Vite token gallery)",
      "runtimeExecutable": "/bin/sh",
      "runtimeArgs": [
        "-c",
        "export PATH=\"/opt/homebrew/bin:/usr/local/bin:$PATH\"; cd \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\" && exec pnpm --filter @scout/docs dev"
      ],
      "port": 5173
    }
  ]
}
