name: Deploy docs to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one in-flight deploy; never cancel an active deploy mid-flight.
concurrency:
  group: pages
  cancel-in-progress: false

# Opt into the new runtime ahead of the June 2026 forced cutover.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    name: Build site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        # Version is read from package.json `packageManager` — don't pass `version` here.
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      # Build the docs site through turbo so every workspace dependency
      # (`@scout/tokens`, `@scout/dropdown`, `@scout/button`, …) is
      # compiled to its `dist/` first. Without this, vite tries to resolve
      # `import '@scout/dropdown'` against a package whose dist has never
      # been generated on a fresh CI checkout, and the build fails with
      # "Failed to resolve entry for package".
      - name: Build docs site (with workspace deps)
        env:
          BASE_PATH: /${{ github.event.repository.name }}/
        run: pnpm turbo run build --filter @scout/docs...

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
